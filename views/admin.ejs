<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>

    <link rel="StyleSheet" href="styles/global.css">
</head>
<body>
    <h1>Submissions</h1>
    <form action="/" method="get">
        <button type="submit">Home</button>
    </form>

    <% if (guests.length === 0) { %>
        <p>No contacts to display!</p>
    <% } else { %>
        <ul>
        <% for (let i = 0; i < guests.length; i++) { %>
            <li><%= guests[i].id %></li>
            <li><%= guests[i].first %></li>
            <li><%= guests[i].last %></li>
            <li><%= guests[i].job %></li>
            <li><%= guests[i].company %></li>
            <li><%= guests[i].linkedin %></li>
            <li><%= guests[i].email %></li>
            <li><%= guests[i].meet %></li>
            <li><%= guests[i].other %></li>
            <li><%= guests[i].message %></li>
            <li><%= guests[i].mailing %></li>
            <li><%= guests[i].format %></li>
            <li><%= guests[i].created_at %></li>
            <hr>
        <% } %> 
        </ul>
    <% } %> 
</body>
</html>